name: hello_world
description: A simple hello world project.
version: 1.0.0+1
authors: James Leahy <defuncart@gmail.com>

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # A package to generate random English words
  english_words: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  # app is rendered using the material design
  uses-material-design: true
